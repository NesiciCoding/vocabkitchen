# Minimal ruff config: catch real bugs, not style opinions.
# The project is dependency-free by design; lint must never require
# dependencies or reformat code (no `ruff format` gate).
[lint]
select = ["E4", "E7", "E9", "F"]
